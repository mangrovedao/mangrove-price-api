import * as dotenv from 'dotenv';
dotenv.config()

import { enableLogging } from '../../src/logger/logger';

enableLogging();

describe("Test monitoring service check server", () => {

  before(() => {
  });

  after(() => {
  });

  beforeEach(() => {
  })

  describe("Test Health Check", () => {
    it("test", () => {

    });
  });

});
